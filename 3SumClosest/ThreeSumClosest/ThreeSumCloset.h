#pragma once
#include <vector>
#include <algorithm>

using namespace std;

class ThreeSumCloset
{
public:
	ThreeSumCloset(void);
	int threeSumClosest(vector<int> &num, int target);
	~ThreeSumCloset(void);
};

