#pragma once
#include <vector>
#include <map>
#include <string>
#include <algorithm>

using namespace std;

class Anagrams
{
public:
	Anagrams(void);
	vector<string> anagrams(vector<string> &strs);
};

